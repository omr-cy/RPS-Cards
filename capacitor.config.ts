import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omr.cardclash',
  appName: 'صراع البطاقات: حجر ورقة مقص',
  webDir: 'dist'
};

export default config;
