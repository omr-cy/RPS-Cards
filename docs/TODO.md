# مشروع Rock Paper Scissors - خريطة الطريق (Roadmap)

## 🏆 نظام نقاط الخبرة (XP) والمستويات (Leveling)

### المرحلة 1: أساسيات النظام (Backend Foundation)
- [x] تحديث نموذج المستخدم (User Model) لإضافة حقول `xp` و `level` و `lastLoginDate/ lastLogin موجود اصلا`.
- [x] تنفيذ خوارزمية المستوى التصاعدي (XP Curve): كل مستوى يتطلب XP أكثر من الذي قبله.
- [x] إضافة منطق مكافأة تسجيل الدخول اليومي (Daily Login XP).
- [x] إضافة نظام مكافآت المباريات (Match Rewards):
    - [x] مكافأة XP عند إنهاء مباراة PvP.
    - [x] مكافأة إضافية (Bonus) عند الفوز.
- [ ] دمج نقاط الخبرة مع نظام المسابقات (Tournament XP): منح مبالغ ضخمة بناءً على التقدم. (يحتاج نظام مسابقات أولاً)

### المرحلة 2: فتح المحتوى وترقية اللاعب (Level-Gated Content)
- [x] تحديث ملف الثيمات (`themes.ts`) لإضافة خاصية `requiredLevel`.
- [x] المتجر: إظهار الثيمات المغلقة (Locked) مع توضيح المستوى المطلوب لفتحها.
- [x] واجهة المستخدم: إضافة شريط تقدم (XP Progress Bar) في القائمة الرئيسية والبروفايل.
- [x] شاشة الارتقاء (Level Up Screen): تصميم نافذة مبهجة تظهر عند وصول اللاعب لمستوى جديد، وتعرض الثيمات التي فُتحت.

### المرحلة 3: لوحة الصدارة العالمية (Leaderboards)
- [x] بناء نقطة نهاية (API Endpoint) لجلب ترتيب اللاعبين:
    - [x] ترتيب بناءً على XP / المستويات.
    - [ ] تصنيف (أسبوعي / شهري / كل الأوقات). (يتطلب نظام تتبع تاريخي)
- [x] تصميم واجهة لوحة الصدارة (Leaderboard UI):
    - [x] عرض أفضل 20 لاعب (قابلة للزيادة).
    - [x] عرض ترتيب اللاعب الشخصي في الأسفل (حتى لو لم يكن في القائمة).
- [ ] إضافة تبديل بين الفلاتر الزمنية في الواجهة.

---

## 🌐 نظام الأونلاين المتقدم (Online Improvements)
- [ ] تحسين نظام البحث العشوائي (Matchmaking) ليكون أسرع وأكثر استقراراً.

## 📱 تحسينات عامة (General Enhancements)
- [ ] تحسين استهلاك البطارية والأداء على الأندرويد.
- [ ] مراجعة ملف الـ `App.tsx` لتقليل حجمه وتنظيم الكود بشكل أفضل (Refactoring).
